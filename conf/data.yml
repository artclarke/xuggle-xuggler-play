# you describe your data using the YAML notation here
# and then load them using Fixtures.load("data.yml")

Person(john):
   firstName: John
   lastName: Doe

Person(jane):
   firstName: Jane
   lastName: Doe